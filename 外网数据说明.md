# 世界杯海外舆情采集代码与数据

本分支保存 2026 世界杯佛得角队相关海外公开数据及其采集代码。筛选范围与仓库中的 `世界杯关键词.md` 一致：

- 日期：2026-05-28 至 2026-07-26（含首尾日期）
- 主题词：佛得角、沃齐尼亚及对应外文写法
- 事件词：世界杯、对阵球队、赛程阶段、晋级与首次参赛等词组
- 排除：篮球、电竞、女足、U17、U20、世俱杯等非目标赛事

## 文件

- `worldcup_external_sources_code.zip`：Guardian、Google News、Bing News、Reddit、Lemmy、Stack Exchange、GDELT、Common Crawl 的采集脚本，以及过滤、合并脚本和配置。
- `worldcup_external_sources_all_data.zip`：全部相关采集结果、断点状态、原始响应和日志，按 `final`、`sources`、`state`、`diagnostics` 分类。
- `worldcup_external_sources_manifest.sha256`：两个 ZIP 包的 SHA-256 校验值。

数据包中的主要最终文件：

| 文件 | 记录数 | 说明 |
| --- | ---: | --- |
| `final/reddit_browser_strict.jsonl` | 10,000 | Reddit 独立数据，含 65 条父帖和 9,935 条评论 |
| `final/worldcup_strict_nonreddit_foreign_only.jsonl` | 6,082 | 不含 Reddit 的海外严格数据 |
| `final/worldcup_strict_with_reddit_foreign_only.jsonl` | 16,082 | Reddit 与其他海外来源合并、按 URL 去重后的数据 |

`worldcup_strict_nonreddit.jsonl` 和 `worldcup_strict_with_reddit.jsonl` 是合并前保留的原始集合，其中可能包括日期不在目标窗口或属于中国大陆来源的条目；分析时应优先使用文件名带 `foreign_only` 的严格结果。

## 数据字段

新闻与论坛通用字段：

- `record_type`：`news_item`、`forum_post` 或 `forum_comment`
- `source`：来源网站或社区
- `url`：记录的唯一公开链接
- `title`：标题；评论可能为空
- `published_at`：发布时间
- `bodyText` / `description`：正文或摘要
- `matched_subject_keywords`：命中的主题词
- `matched_event_keywords`：命中的事件词
- `excluded_keywords`：命中的排除词，严格结果中应为空
- `discovery_url`：检索或接口来源

Reddit 评论额外包含 `parent_url`、`parent_title`、`parent_publication_date`；Guardian 原始记录使用 `webTitle`、`webPublicationDate`、`trailText`、`section`、`type` 等官方 API 字段。

## 使用方法

解压代码包，安装 Python 依赖：

```powershell
python -m pip install requests beautifulsoup4
```

各脚本均支持 `--help`。Guardian 需要官方 API Key；Reddit 浏览器采集脚本需在已登录 Reddit 的 Playwright 浏览器会话中运行。任何 Cookie、API Key 和本地账号数据库均未提交。

合并和海外来源过滤示例：

```powershell
python merge_strict_sources.py --inputs data/source1.jsonl data/source2.jsonl --output data/merged.jsonl
python filter_foreign_sources.py --input data/merged.jsonl --output data/foreign_only.jsonl --start-date 2026-05-28 --end-date 2026-07-26
```

采集结果来自公开页面或公开接口。使用时应遵守相应平台条款、robots 规则和适用法律，不应将评论数据用于识别个人。
